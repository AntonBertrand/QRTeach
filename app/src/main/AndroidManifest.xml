<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qrteach">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QRTeach">
        <activity android:name=".getName"></activity>
        <activity android:name=".Messages" />
        <activity android:name=".Chat" />
        <activity android:name=".HelpSheet" />
        <activity android:name=".QRCode" />
        <activity android:name=".CreateSheet" />
        <activity android:name=".TeacherMenu" />
        <activity android:name=".QRScanner" />
        <activity android:name=".UserMenu" />
        <activity android:name=".ForgotPassword" />
        <activity android:name=".RegisterUser" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>